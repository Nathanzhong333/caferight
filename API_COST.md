# API 使用和成本控制指南

## 高德地图 API 计费说明

### 免费服务（我们正在使用的）

当前项目使用的功能**完全免费**：

1. **JavaScript API 地图加载** - 免费
   - 用户打开地图
   - 地图缩放、拖动、旋转
   - 地图样式切换

2. **标记点（Marker）** - 免费
   - 在地图上显示咖啡店位置
   - 自定义标记图标
   - 标记点击事件

3. **信息窗口（InfoWindow）** - 免费
   - 点击标记显示详情

### 付费服务（我们暂不使用）

以下服务会产生费用，项目当前**没有使用**：

- ❌ Web 服务 API（路线规划、地理编码、搜索等）
- ❌ 导航服务
- ❌ POI 搜索
- ❌ 地址解析

### 免费配额

| 认证类型 | 每日免费调用次数 |
|---------|----------------|
| 个人开发者认证 | 100,000 次/天 |
| 企业认证 | 300,000 次/天 |

## 当前项目的 API 调用分析

### 单个用户打开应用的调用次数

```
用户打开应用 = 1 次地图加载
├── 加载地图 JS 库（一次性，浏览器缓存）
├── 初始化地图（免费）
├── 加载 5 个咖啡店标记（免费）
└── 显示地图瓦片（免费）

实际 API 调用次数 = 0 次（Web 服务 API）
```

**重点**：加载地图和显示标记**不消耗 Web 服务 API 配额**！

### 实际场景计算

假设您的应用：
- 每天 1000 个用户访问
- 每个用户打开地图 1 次

**消耗的配额** = 0 次（因为只使用了 JS API，不是 Web 服务 API）

## 优化方案

虽然当前免费，但我们仍然可以优化性能和用户体验：

### 1. 咖啡店数据优化

**当前方案**：咖啡店数据存储在前端 JSON 文件中
- ✅ 优点：不调用 API，完全免费
- ❌ 缺点：数据更新需要重新部署

**未来优化**：使用自己的后端数据库
```
前端 -> 自己的后端 API -> 返回咖啡店数据
                      -> （可选）调用高德地图 API
```

### 2. 地图加载优化

```javascript
// 懒加载地图（用户需要时才加载）
const Map = lazy(() => import('./components/Map'));

// 或者：按需加载
{showMap && <Map coffeeShops={coffeeShops} />}
```

### 3. 缓存策略

```javascript
// 地图 JS 库会被浏览器自动缓存
// 用户第二次访问时，不会重新下载地图库
```

### 4. 使用静态坐标数据

当前已实现：咖啡店的经纬度坐标已经存储在数据文件中
- 不需要调用地理编码 API（这个服务是收费的）
- 直接在地图上显示

### 5. 按地区分片加载

当咖啡店数量增多时：
```javascript
// 只加载用户可见区域的咖啡店
map.on('moveend', () => {
  const bounds = map.getBounds();
  const visibleShops = filterShopsByBounds(bounds);
  updateMarkers(visibleShops);
});
```

## 成本预估

### 场景 1：个人项目/小型应用
- 每天访问量：< 1000 人
- **成本**：0 元（完全在免费额度内）

### 场景 2：中型应用
- 每天访问量：10,000 人
- 使用功能：地图展示 + 标记
- **成本**：0 元（JS API 免费）

### 场景 3：大型应用（未来扩展）
如果需要以下功能，才会产生费用：
- 路线规划（从用户位置到咖啡店）
- POI 搜索（"附近的咖啡店"）
- 地理编码（地址转坐标）

**解决方案**：
1. 使用后端代理，控制调用频率
2. 添加缓存层，相同请求不重复调用
3. 考虑购买高德地图的商业授权

## 如何监控 API 使用

1. 登录 [高德开放平台控制台](https://console.amap.com/)
2. 查看"数据统计"
3. 监控每日调用量
4. 设置用量告警

## 成本控制建议

### 立即实施
- ✅ 使用静态咖啡店数据（已实现）
- ✅ 不调用搜索、地理编码等付费 API（已实现）
- ✅ 设置域名白名单防止滥用（已说明）

### 未来扩展时
- 🔄 搭建自己的后端 API
- 🔄 实现数据缓存
- 🔄 使用 Redis 缓存热点数据
- 🔄 如果需要付费服务，考虑按需加载

## 替代方案

如果担心成本，也可以考虑：

1. **百度地图 API** - 也有免费额度
2. **腾讯地图 API** - 个人开发者免费
3. **开源地图** - Leaflet + OpenStreetMap（完全免费，但国内访问较慢）

## 总结

✅ **好消息**：当前应用的所有功能都是免费的！
✅ **免费额度**：10万次/天（个人）足够使用
✅ **优化方向**：使用静态数据，避免调用付费 API
✅ **成本控制**：设置白名单和限额，后端代理

**只要不使用路线规划、搜索、地理编码等 Web 服务 API，就完全免费！**
