# 云端部署指南

## 🚀 方案一：Vercel 部署（推荐，最简单）

### 为什么选择 Vercel？

- ✅ **完全免费**
- ✅ **3分钟部署**
- ✅ **自动 HTTPS**
- ✅ **全球 CDN 加速**
- ✅ **支持环境变量**（可配置地图 API Key）
- ✅ **自动部署**（代码更新后自动重新部署）

### 部署步骤

#### 1. 注册 Vercel 账号

访问 https://vercel.com/ 并注册（推荐使用 GitHub 账号登录）

#### 2. 导入项目

1. 点击 "New Project"
2. 选择 "Import Git Repository"
3. 授权 Vercel 访问您的 GitHub
4. 选择 `caferight` 仓库
5. 点击 "Import"

#### 3. 配置项目

Vercel 会自动识别为 Vite 项目，使用默认配置即可：

- **Framework Preset**: Vite
- **Build Command**: `npm run build`
- **Output Directory**: `dist`

#### 4. 配置环境变量（可选，启用地图功能）

在部署前添加环境变量：

1. 展开 "Environment Variables"
2. 添加变量：
   - **Name**: `VITE_AMAP_KEY`
   - **Value**: 您的高德地图 API Key
3. 点击 "Add"

#### 5. 部署

点击 "Deploy" 按钮，等待 1-2 分钟。

#### 6. 完成！

部署成功后，您会获得一个访问地址，类似：
```
https://caferight.vercel.app
```

或自定义域名：
```
https://your-custom-domain.com
```

### 更新应用

每次您推送代码到 GitHub 仓库，Vercel 会自动重新部署！

### 配置自定义域名（可选）

1. 在 Vercel 项目设置中找到 "Domains"
2. 添加您的域名
3. 按提示配置 DNS

---

## 🌐 方案二：Netlify 部署

### 部署步骤

#### 1. 注册 Netlify

访问 https://netlify.com/ 并注册

#### 2. 导入项目

1. 点击 "Add new site" > "Import an existing project"
2. 选择 GitHub，授权访问
3. 选择 `caferight` 仓库

#### 3. 配置构建设置

- **Build command**: `npm run build`
- **Publish directory**: `dist`

#### 4. 配置环境变量

在 "Site settings" > "Environment variables" 添加：
- **Key**: `VITE_AMAP_KEY`
- **Value**: 您的高德地图 API Key

#### 5. 部署

点击 "Deploy site"，完成后获得类似地址：
```
https://caferight.netlify.app
```

---

## 📄 方案三：GitHub Pages 部署

### 步骤 1：修改配置

在项目根目录创建或修改 `vite.config.ts`：

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/caferight/', // 改为您的仓库名
})
```

### 步骤 2：添加部署脚本

在 `package.json` 中添加：

```json
{
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

安装 gh-pages：
```bash
npm install -D gh-pages
```

### 步骤 3：部署

```bash
npm run deploy
```

### 步骤 4：配置 GitHub Pages

1. 进入仓库 Settings > Pages
2. Source 选择 `gh-pages` 分支
3. 访问地址：`https://yourusername.github.io/caferight/`

**注意**：GitHub Pages 不支持环境变量，需要直接在代码中配置 API Key（不推荐，有安全风险）

---

## 🖥️ 方案四：云服务器部署（进阶）

### 适合场景

- 需要后端服务
- 需要数据库
- 需要完全控制

### 云服务商选择

| 服务商 | 最低价格 | 特点 |
|--------|---------|------|
| **阿里云** | ~￥60/月 | 国内速度快 |
| **腾讯云** | ~￥60/月 | 新用户优惠 |
| **华为云** | ~￥60/月 | 企业级 |
| **DigitalOcean** | $6/月 | 国际服务 |

### 部署步骤（以 Ubuntu 服务器为例）

#### 1. 连接服务器

```bash
ssh root@your-server-ip
```

#### 2. 安装 Node.js

```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs
```

#### 3. 安装 Nginx

```bash
apt update
apt install nginx -y
```

#### 4. 克隆项目

```bash
cd /var/www
git clone https://github.com/Nathanzhong333/caferight.git
cd caferight
```

#### 5. 安装依赖并构建

```bash
npm install
npm run build
```

#### 6. 配置 Nginx

创建配置文件 `/etc/nginx/sites-available/caferight`：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/caferight/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

启用配置：

```bash
ln -s /etc/nginx/sites-available/caferight /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

#### 7. 配置 HTTPS（使用 Let's Encrypt）

```bash
apt install certbot python3-certbot-nginx -y
certbot --nginx -d your-domain.com
```

---

## 🔐 安全提醒

### 对于所有部署方式：

1. **配置高德地图域名白名单**
   - 登录高德地图控制台
   - 添加您的部署域名（如 `caferight.vercel.app`）

2. **使用环境变量**
   - 不要在代码中直接写 API Key
   - Vercel 和 Netlify 都支持环境变量

3. **设置调用限制**
   - 在高德控制台设置每日调用上限
   - 防止恶意使用

---

## 📊 部署方案对比

| 特性 | Vercel | Netlify | GitHub Pages | 云服务器 |
|------|--------|---------|--------------|---------|
| **费用** | 免费 | 免费 | 免费 | ￥60+/月 |
| **难度** | ⭐ | ⭐ | ⭐⭐ | ⭐⭐⭐⭐ |
| **自动部署** | ✅ | ✅ | ❌ | ❌ |
| **环境变量** | ✅ | ✅ | ❌ | ✅ |
| **HTTPS** | ✅ | ✅ | ✅ | 需配置 |
| **CDN** | ✅ | ✅ | ✅ | ❌ |
| **自定义域名** | ✅ | ✅ | ✅ | ✅ |
| **后端支持** | 有限 | 有限 | ❌ | ✅ |

---

## 🎯 推荐选择

### 个人项目/展示

👉 **选择 Vercel** - 最简单，功能最全

### 需要后端

👉 **云服务器** - 完全控制

### 预算有限

👉 **Vercel/Netlify** - 完全免费

---

## ❓ 常见问题

### Q: 部署后地图不显示？

A: 需要配置环境变量 `VITE_AMAP_KEY`，并在高德控制台添加部署域名到白名单

### Q: 可以同时部署到多个平台吗？

A: 可以！比如 Vercel 用于生产环境，Netlify 用于测试

### Q: 部署需要花钱吗？

A: Vercel 和 Netlify 对个人项目完全免费

### Q: 域名需要备案吗？

A: 使用 Vercel/Netlify 提供的子域名不需要，使用自己的域名需要根据服务器位置决定

### Q: 如何更新部署的应用？

A: 推送代码到 GitHub，Vercel/Netlify 会自动重新部署

---

## 🚀 开始部署

最简单的方式：

1. 访问 https://vercel.com/
2. 点击 "New Project"
3. 导入 `caferight` 仓库
4. 点击 "Deploy"
5. 等待 1-2 分钟
6. 完成！获得访问地址

立即开始，3分钟就能让全世界访问您的咖啡地图！
